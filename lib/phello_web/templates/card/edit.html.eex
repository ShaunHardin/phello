<h2>Edit card</h2>

<%= render "form.html", changeset: @changeset,
                        action: board_card_path(@conn, :update, @card.board_id, @card) %>

<%= link "Back", to: board_path(@conn, :index) %>
