<h2>New card</h2>

<%= render "form.html", changeset: @changeset,
                        action: board_card_path(@conn, :create, @changeset.data.board_id) %>

<%= link "Back", to: board_path(@conn, :index) %>
